<?xml version="1.0" encoding="UTF-8"?>
<b:beans xmlns="http://www.springframework.org/schema/security"
         xmlns:b="http://www.springframework.org/schema/beans"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"

         xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/security
        http://www.springframework.org/schema/security/spring-security.xsd
">
    <b:bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <b:property name="driverClassName" value="org.apache.derby.jdbc.ClientDriver"/>
        <b:property name="url" value="jdbc:derby://localhost:1527/course"/>
        <b:property name="username" value="sergio"/>
        <b:property name="password" value="sergio"/>
    </b:bean>

    <!--Spring Security-->
    <authentication-manager>
        <authentication-provider>
            <jdbc-user-service data-source-ref="dataSource"
                               users-by-username-query=
                                       "SELECT username, password, true FROM USER_INFO
                                       WHERE username=?"
                               authorities-by-username-query=
                                       "SELECT username, user_role FROM USER_INFO
                                       WHERE username=?"/>
        </authentication-provider>
    </authentication-manager>

    <http auto-config="true">

        <intercept-url pattern="/lecture/edit/*" access="hasAnyRole('LECTURER','ADMIN')"/>
        <intercept-url pattern="/lecture/edit" access="hasAnyRole('LECTURER','ADMIN')"/>
        <intercept-url pattern="/lecture/**" access="isAuthenticated()"/>
        <intercept-url pattern="/createAccount" access="hasRole('ADMIN')"/>

        <remember-me
                key="uniqueAndSecret"
                token-validity-seconds="86400"
                remember-me-parameter="remember-me"/>
    </http>
</b:beans>